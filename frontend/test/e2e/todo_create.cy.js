describe("Todo create", () => {
    beforeEach(() => {
        cy.visit("http://localhost:3000/");
        cy.get("#email").type("kroh24@student.bth.se{enter}");
    })
    // it("Create Task", () => {
    // // make sure the landing page contains a header with "login"
    // cy.get("#title").type("cool title{enter}");
    // });

    // create Todo
})